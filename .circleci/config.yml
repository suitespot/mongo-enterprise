version: 2

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.18
      - run:
          name: Build application Docker image
          command: |
            docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_PASSWORD}
            docker context ls
            docker buildx create --name mybuilder --use
            docker buildx ls
            # linux/arm/v7,linux/arm64/v8,
            docker buildx build . --platform linux/amd64 -t suitespottechnology/mongo-enterprise:latest --push

workflows:
  version: 2
  build_and_push:
    jobs:
      - build:
          context: org-global
